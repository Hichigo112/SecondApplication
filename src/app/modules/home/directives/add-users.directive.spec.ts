import { AddUsersDirective } from './add-users.directive';

describe('AddUsersDirective', () => {
  it('should create an instance', () => {
    const directive = new AddUsersDirective();
    expect(directive).toBeTruthy();
  });
});
